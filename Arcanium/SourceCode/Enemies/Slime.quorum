class Slime is Enemy

    on create
        SetStats("Slime", 100, 0, 10, 0.25, 0.0, 0.35, 0.2, 1, 1)
    end

    action AI
        Spell spell
        spell:SetID(9010000)
        spell:ParseJSON()

        SetSpell(spell)
    end
end